package com.br.samples.api.camerax

import android.content.Context

object CameraXProvider {



    fun providerCamera(context: Context) {
       // val cam = ProcessCameraProvider.getInstance()
    }
}