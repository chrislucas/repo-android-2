package viewpage.xplorer.com.xplorerviewpager.animation.viewpager.adapter;

import android.support.annotation.NonNull;
import android.support.annotation.Nullable;
import android.support.v4.view.PagerAdapter;
import android.support.v4.view.ViewPager;

public class ImplOnAdapterChangeListener implements ViewPager.OnAdapterChangeListener {

    /**
     * Um listener quando a ViewPager troca de adapter
     * */

    @Override
    public void onAdapterChanged(@NonNull ViewPager viewPager
            , @Nullable PagerAdapter oldAdapter, @Nullable PagerAdapter newAdapter) {

    }
}
