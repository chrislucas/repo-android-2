<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="jobservice.xplorer.com.br.anotherstudyaboutalarmmanager">
  
  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".TestRyLib">

    </activity>
    <activity android:name=".MainActivity" />
    
    <receiver
      android:name=".broadcast.BroadcastReceiverFilterAlarm"
      android:enabled="true"
      android:exported="true"
      />
    
    <activity android:name=".SimpleExampleInexactlyAlarm">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    
    <service
      android:name=".services.ServiceFilterAlarm"
      android:exported="false"
      />
  </application>

</manifest>